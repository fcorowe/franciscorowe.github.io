---
title: "Template"
draft: true
author: "Francisco Rowe"
date: '2020-01-01'
categories: ["R"]
tags: ["R Markdown", "Migration", "Spatial Impact"]
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(collapse = TRUE)
```

# R Markdown

This file provides a template to publish a post based on R markdown including code chunks. If you want to publish the content of this R markdown on the website, follow these steps:

Publishing a post with Hugo

1. set the working directory `setwd()`

2. run `blogdown::serve_site()`

3. push it via terminal to GitHub and the website


## Installing
```{r}
library(CIM)
```

```{r}
## Read in the two data.frames included in the package
m <- male
f <- female

## Run the function using "ratio" calculation
CIM.ratio <- CIM(m, f, calculation = "ratio", numerator = 1, denominator = 2)
## Print the resulted data.frame
CIM.ratio

```

